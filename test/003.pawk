
from math import sqrt

def mysub(x, y):
	return x - y

class Test:
	def test(self, x, y):
		return mysub(x, y) - (x - y)

with mysub(F[1], 0):
	print(int(mysub(sqrt(F[1]), F[2])))
	assert Test().test(F[1], F[2]) == 0
